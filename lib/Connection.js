// Generated by CoffeeScript 1.6.3
(function() {
  var Channel, Connection;

  Channel = require('./Channel');

  Connection = (function() {
    function Connection(properties) {
      if (properties == null) {
        properties = {};
      }
      this.uri = properties.uri;
      this.wrapped = properties.wrapped;
    }

    Connection.prototype.createChannel = function(properties, callback) {
      return this.wrapped.createChannel(function(error, wrapped) {
        var channel;
        if (error != null) {
          return callback(error);
        }
        channel = new Channel(properties);
        channel.wrapped = wrapped;
        return callback(null, channel);
      });
    };

    Connection.prototype.close = function(callback) {
      return this.wrapped.close(function(error) {
        return callback(error);
      });
    };

    return Connection;

  })();

  module.exports = Connection;

}).call(this);

/*
//@ sourceMappingURL=Connection.map
*/
